{
  "name": "common",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --watch --preserveWatchOutput"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  }
}
