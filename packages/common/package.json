{
  "name": "common",
  "main": "./dist/index",
  "types": "./dist/index",
  "scripts": {
    "build": "tsc --build --force tsconfig.json",
    "dev": "tsc --watch --preserveWatchOutput"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "tsconfig": "workspace:^"
  }
}
